
create table reabertura_op_304 (
  cod_empresa       char(02),
  num_op            integer,
  motivo            char(80),
  usuario           char(08),
  data              date
);

create index ix1_reabre_op on reabertura_op_304
 (cod_empresa, num_op);

  
