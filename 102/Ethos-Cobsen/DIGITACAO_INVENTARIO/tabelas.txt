

DROP table itens_invent_912;	
	
create table itens_invent_912
  (
    cod_empresa char(2) not null ,
    cod_item char(15) not null ,
    num_contagem smallint not null ,
    local_fabrica char(30) not null ,
    cod_local CHAR(10) NOT NULL,
    ies_dimensional CHAR(1) NOT NULL,
    num_lote CHAR(15),
    endereco CHAR (15),
    num_volume INTEGER,
    dat_hor_producao DATETIME year TO second, //SQL SERVER : DATETIME 
    dat_hor_validade DATETIME year TO second,//SQL SERVER : DATETIME 
    comprimento DECIMAL (15, 3),
    largura DECIMAL (15, 3),
    altura DECIMAL (15, 3),
    diametro DECIMAL (15, 3),
    num_peca CHAR (15),
    num_serie CHAR (25),
    qtd_contada decimal(15,3) not null ,
    ies_situa_qtd char(1) not null,
    num_folha integer NOT NULL,
    cod_usuario char(50) NOT NULL,
    dat_hor_inclusao DATETIME year TO second NOT NULL //SQL SERVER : DATETIME 
  );  
  
CREATE INDEX ix_itens_invent_912 ON itens_invent_912(cod_empresa,cod_item,ies_dimensional);

CREATE INDEX ix2_itens_invent_912 ON itens_invent_912 (cod_empresa,cod_item,num_contagem,ies_situa_qtd,cod_local,local_fabrica,num_lote); 


INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_local_fabrica', 'N', 'Utiliza o campo local fábrica no inventário.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_ver_duplic', 'S', 'Verifica duplicidade na digitação do inventário.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_lote_padrao', 'S', 'Utiliza o mesmo lote para todos os itens.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_val_lote_padrao', '07062013', 'Valor do lote para todos os itens.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_ver_lista_dupl', 'N', 'Verifica se o item já foi digitado em outra lista.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_usuario_so', 'N', 'Utiliza o usuário do windows. Senão usa do Plugin');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_lista_unica', 'S', 'Utiliza a lista como única por contagem.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_ult_cont_lista', 'N', 'Utiliza a últ. contagem por lista, na exportação.');

INSERT INTO sistema_par (nom_parametro, val_parametro, obs)
VALUES ('inv_serie_ife', 'N', 'Utiliza a leitura de etiquetas da IFE');
